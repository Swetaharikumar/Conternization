

import Operations as op

operation = op.Operations()
config_files = {"files": []}
needed_config = ['name', 'major', 'minor', 'base_image', 'mounts',
                 'startup_script', 'startup_owner', 'startup_env']

instance_info = {"instances": []}
